apiVersion: v2
name: apigee-cassandra
description: |
  Wrapper chart around Apigee Hybrid Cassandra (ApigeeDatastore CR).
  Pilote la topologie via values-topology.yaml (phase 1 mono-DC → phase 2 multi-DC).
type: application
version: 0.1.0
appVersion: "1.14.0"    # version Apigee Hybrid ciblée

# Pas de dépendance Helm : on génère directement le CR ApigeeDatastore
# (et non le chart officiel, qui ferait trop de choses d'un coup pour ArgoCD).

maintainers:
  - name: Équipe Apigee
    email: apigee-team@example.com
